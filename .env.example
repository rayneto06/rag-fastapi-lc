###########################################
# RAG FastAPI + LangChain + LangSmith
# -----------------------------------------
# Copie este arquivo para `.env`
# e ajuste as variáveis conforme seu ambiente.
###########################################

# -----------------------------------------
# APP SETTINGS
# -----------------------------------------
# Caminhos e configuração geral
VECTOR_STORE_PROVIDER=chroma
CHROMA_DIR=.chroma
RAW_DIR=data/raw
CHROMA_COLLECTION=documents

# -----------------------------------------
# EMBEDDINGS (R)
# -----------------------------------------
# Provider: fake | ollama | openai
EMBEDDINGS_PROVIDER=ollama
# Modelo de embeddings (Ollama: nomic-embed-text)
EMBEDDINGS_MODEL=nomic-embed-text

# -----------------------------------------
# LLM (G)
# -----------------------------------------
# Provider: fake | ollama | openai
LLM_PROVIDER=ollama
# Modelo de linguagem (ex: llama3.1, mistral, gpt-4o-mini)
LLM_MODEL=llama3.1
# Temperatura (0.0 = determinístico, >0 = mais criativo)
LLM_TEMPERATURE=0.0

# -----------------------------------------
# RETRIEVER DEFAULTS
# -----------------------------------------
RETRIEVER_SEARCH_TYPE=mmr
RETRIEVER_K=5

# -----------------------------------------
# LANGSMITH (Observabilidade)
# -----------------------------------------
# Para habilitar tracing de execuções no LangSmith:
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=    # coloque sua chave LangSmith, se desejar
LANGSMITH_PROJECT=rag-fastapi-lc

# -----------------------------------------
# OLLAMA (Local LLM)
# -----------------------------------------
# Ollama precisa estar rodando localmente com `ollama serve`
# Padrão: http://localhost:11434
OLLAMA_BASE_URL=http://localhost:11434

# Exemplos de modelos:
#   ollama pull llama3.1
#   ollama pull nomic-embed-text

# -----------------------------------------
# OPENAI (caso queira usar)
# -----------------------------------------
# Para usar OpenAI, altere LLM_PROVIDER=openai e forneça:
# LLM_MODEL=gpt-4o-mini (ou outro modelo)
OPENAI_API_KEY=

# -----------------------------------------
# TEST SETTINGS
# -----------------------------------------
# Use `pytest -q -m integration` para rodar testes reais
# e `pytest -q -m "not integration"` para testes determinísticos
###########################################
